@model PhamQuangLong_1911067466_LAB456.ViewModels.FollowingViewModel
@{
    ViewBag.Title = "Following Me";
}

<div class="row">
    <ul class="course">
        @foreach (var follow in Model.Followings)
        {
            <li>
                <div class="details">
                    <span class="lecturer">
                        @follow.Name       
                    </span>
                </div>
                <br />
            </li>
        }
    </ul>
</div>
